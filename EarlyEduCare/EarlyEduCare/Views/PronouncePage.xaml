<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="EarlyEduCare.Views.PronouncePage" Title="Words"  Icon="dictionary.png">

    <Frame BorderColor="#278fd7" CornerRadius="5" Padding="8" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" WidthRequest="300" Margin="20" HeightRequest="250">
        <StackLayout>
            <Label Text="The word : " FontSize="Medium" FontAttributes="Bold" TextColor="#278fd7" Padding="20"/>

            <Label Text="{Binding Word}" FontSize="Medium" Padding="20,0"></Label>
            <BoxView Color="#278fd7" HeightRequest="2" HorizontalOptions="Fill" Margin="20,0"/>
            <Label Text="Defination" FontSize="Medium" FontAttributes="Bold" TextColor="#278fd7" Padding="20"/>

            <Label Text="{Binding Definition}" FontSize="Medium" Padding="20,0"></Label>
           
            <Button Text="Get A Word" Command="{Binding TestTwoCommand}"  WidthRequest="300" Padding="10" BackgroundColor="#278fd7" HorizontalOptions="Center" Margin="20,120,20,0" CornerRadius="10" FontSize="Medium" TextColor="White" />
           
        </StackLayout>
    </Frame>

</ContentPage>